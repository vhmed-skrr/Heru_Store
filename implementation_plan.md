# خطة تحسين الأداء (Web Vitals) وتوافق الـ SEO لمتجر Heru

بناءً على التوصيات والمعايير التقنية الصارمة لمحركات البحث، سيتم وضع خطة شاملة كالتالي:

## 1. تحديثات الصفحات الأساسية (Meta & Open Graph)
سيتم حقن كل الصفحات الآتية: `index.html, shop.html, product.html, review.html وغيرها` بـ:
- `title` ذكي وفريد لتجنب التكرار.
- `meta description` تعريفي لزيادة الـ Click Through Rate.
- `Canonical Tag` لمنع محركات البحث من اعتبار الروابط المتشابهة (بسبب الـ Query Parameters) محتوى مكرر.
- بطاقات وسائل التواصل الاجتماعي `og:image` و `twitter:card`.

## 2. تحسينات الأداء وتجربة المستخدم (Performance)
- **Lazy Loading:** سيتم البحث عن جميع صور المتجر `<img>` وتطبيق سمات `loading="lazy"` مع تحديد أبعاد دقيقة (Width/Height) لمنع اهتزاز الشاشة (Cumulative Layout Shift) قبل التحميل. باستثناء صورة بطليّ (Hero) في المتجر ستكون `eager`.
- **ترتيب ملفات الـ CSS والـ JS:** التأكد من تضمين ستايل `tokens → base → components` على جميع الصفحات، ودفع استدعاءات `script` لأخر مسار وتفعيل خاصية `defer` لملفات `data.js`.

## 3. تحجيم صور Cloudinary ديناميكاً
- سنُنشئ دالة بملف الملحقات البرمجية لتحويل روابط Cloudinary الأصلية التي تعود من قاعدة البيانات إلى روابط مضغوطة أوتوماتيكياً:
  - للصور المصغرة: `/upload/w_400,q_auto,f_auto/`
  - للصور المعروضة للـ Product: `/upload/w_800,q_auto,f_auto/`
  - لصور النشر على وسائل التواصل الـ OG: `/upload/w_1200,h_630,c_fill,q_auto,f_auto/`

## 4. المخططات الوصفية (JSON-LD)
- بداخل `product.html` ومسار الكود الخاص باستدعاء صفحة المنتج، سيقوم الـ Javascript بعد جلب المنتج، برسم سكربت `application/ld+json` داخل الـ DOM. مما يضمن ظهور تقييم وأسعار منتجاتك في نتائج بحث Google الغنية (Rich Snippets).

## 5. خريطة الروابط (Sitemap & Robots.txt)
- نظراً لأن موقعنا Front-end وحيد (Static)، سنقوم بإنشاء ملف كود محلي (Node Script) يمكن الركض عليه مستقبلاً `generate-sitemap.js` ليولد الروابط الديناميكية، بالإضافة لملف `sitemap.xml` ثابت أساسي سيتم ربطه في ملف `robots.txt` لتتتبعه العناكب.

---
> [!IMPORTANT]  
> هل تفضل أن يتم إجراء هذه التحديثات الطويلة دفعة واحدة عبر ملفات الـ HTML أم لديك تعقيبات مسبقة قبل أن أشرع في التطبيق؟ يرجى إعطاء الضوء الأخضر للبدء.
